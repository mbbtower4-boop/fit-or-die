# הקמת Firebase ל-Fit or DIE! 💀🔥

מדריך חד-פעמי (~15 דקות). בסופו האפליקציה עוברת ממצב הדגמה למצב ענן אמיתי משותף לכל הקבוצה. הכול במסלול החינמי (Spark) — לא נדרש כרטיס אשראי.

## שלב 1 — יצירת פרויקט

1. נכנסים אל https://console.firebase.google.com עם חשבון הגוגל שלך.
2. **Create a project** → שם: `fit-or-die` → אפשר לכבות את Google Analytics (לא נחוץ) → **Create**.

## שלב 2 — הפעלת התחברות באימייל וסיסמה

1. בתפריט הצד: **Build → Authentication → Get started**.
2. לשונית **Sign-in method** → לוחצים על **Email/Password** → מעבירים ל-**Enable** → **Save**.

## שלב 3 — יצירת מסד הנתונים

1. בתפריט הצד: **Build → Firestore Database → Create database**.
2. מיקום: לבחור `eur3 (europe-west)` (הכי קרוב לישראל) → **Next**.
3. מצב: **Start in production mode** → **Create**.

## שלב 4 — הדבקת חוקי האבטחה

1. בלשונית **Rules** של Firestore.
2. מוחקים את התוכן הקיים ומדביקים את כל התוכן של הקובץ `firestore.rules` שבתיקייה הזו.
3. **Publish**.

## שלב 5 — רישום אפליקציית ווב וקבלת ההגדרות

1. לוחצים על גלגל השיניים ⚙️ ליד **Project Overview** → **Project settings**.
2. בתחתית, תחת **Your apps**, לוחצים על סמל הווב **`</>`**.
3. שם: `fit-or-die` → לא לסמן Hosting → **Register app**.
4. מוצג קטע קוד עם אובייקט `firebaseConfig` — מעתיקים **רק את האובייקט**, למשל:

```js
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "fit-or-die-xxxx.firebaseapp.com",
  projectId: "fit-or-die-xxxx",
  storageBucket: "fit-or-die-xxxx.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef"
};
```

## שלב 6 — חיבור האפליקציה

1. פותחים את `index.html` ומאתרים בתחילת הסקריפט את השורה:
   `const FIREBASE_CONFIG = null;`
2. מחליפים את `null` באובייקט שהעתקתם:
   `const FIREBASE_CONFIG = { apiKey: "AIza...", ... };`
3. שומרים. מרעננים את הדף — יופיע מסך התחברות במקום מצב ההדגמה.

הערה: מותר וזה בסדר שההגדרות האלה גלויות בקוד וב-GitHub — ה-apiKey של Firebase לצד לקוח **אינו סוד** (הוא מזהה פרויקט, לא מפתח גישה); ההגנה האמיתית היא חוקי האבטחה משלב 4 + ההתחברות.

## שלב 7 — יציאה לדרך עם הקבוצה

1. אתה נרשם ראשון (אימייל+סיסמה) → **יצירת אתגר חדש** → שם, קוד הזמנה שאתה ממציא (למשל `FIT24`), משך ויעד שבועי.
2. שולחים לחברים את כתובת האפליקציה + קוד ההזמנה.
3. כל חבר: נרשם → **הצטרפות לאתגר** → מזין את הקוד → בונה את כרטיס הגיבור שלו.
4. כל אחד מתקין למסך הבית (הוראות בתוך האפליקציה: הגדרות → התקנה על האייפון).

## מגבלות המסלול החינמי (בשביל 4 משתתפים — שוליים ענקיים)

| משאב | מכסה חינמית ליום | שימוש צפוי של 4 משתתפים |
|---|---|---|
| קריאות מסמכים | 50,000 | מאות עד אלפים בודדים |
| כתיבות | 20,000 | עשרות |
| אחסון כולל | 1GB | תמונות דחוסות ~150KB ⇒ אלפי תמונות |
