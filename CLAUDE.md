# CLAUDE.md — fit-or-die

אפליקציית אתגר כושר קבוצתי "Fit or DIE!" — עברית RTL, קובץ יחיד.

## מבנה

- **כל האפליקציה (CSS+HTML+JS) בקובץ `index.html`** — ונילה, ללא build, בסגנון REPORTpro/my-money-app. להריץ: לפתוח בדפדפן.
- `SPEC.md` — מסמך האפיון וההחלטות המוצריות. `SETUP-FIREBASE.md` — מדריך הקמה למשתמש.
- `firestore.rules` — חוקי האבטחה; כל שינוי במבנה הנתונים מחייב עדכון גם שם (המשתמש מדביק אותם ידנית בקונסולת Firebase — לציין לו כשצריך).

## ארכיטקטורה

- שני מימושי שכבת נתונים עם אותו API: `DemoStore` (localStorage + נתוני דמו) ו-`FireStore` (Firebase compat SDK 10.14.1 נטען דינמית מ-CDN). הבחירה לפי `FIREBASE_CONFIG` (null ⇒ דמו). **כל פיצ'ר חדש חייב להיתמך בשני המימושים.**
- `applyCheckin` הוא מנוע המשחוק — פונקציה טהורה, מקור אמת יחיד לחוקי XP/תגים/רצפים; נתוני הדמו נבנים דרכו כדי שהצבירות יהיו עקביות.
- רינדור: `state` גלובלי + פונקציות `*HTML()` שמזינות את `app.innerHTML`; אירועים ב-onclick גלובלי (כמו REPORTpro).
- שבוע ישראלי (ראשון–שבת); `curWeek()` נגזר מ-`startDate`. ערכת נושא ברירת-מחדל מתחלפת לפי מספר השבוע.
- נתונים פרטיים (משקל/תמונות) — תת-מסמך `members/{uid}/private/data`, נגיש רק לבעליו ברמת ה-rules.
- תמונות: דחיסה ל-JPEG data URL (עד ~250KB) ושמירה בתוך מסמכי Firestore. אין Cloud Storage (דורש חיוב).

## קונבנציית שחרור

לכל שינוי גלוי למשתמש: לעדכן `APP_VERSION` ב-index.html **וגם** `version` ב-package.json (זהים), רשומה עברית מתוארכת ב-CHANGELOG.md, והגרסה בהודעת הקומיט. הגרסה מוצגת בלוגו ובהגדרות.

## פריסה

GitHub Pages מהשורש (כמו שאר הפרויקטים). ה-URL ציבורי אך התוכן מאחורי Auth + קוד הזמנה. `FIREBASE_CONFIG` בקוד אינו סוד — ההגנה בחוקי האבטחה.
